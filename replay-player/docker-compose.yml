services:
  replay-player:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: bash -lc "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
